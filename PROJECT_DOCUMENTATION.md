# FindShroom - Полная Документация Проекта

## 📋 Содержание
1. [Обзор проекта](#обзор-проекта)
2. [Архитектура](#архитектура)
3. [Система пользователей](#система-пользователей)
4. [Премиум подписка](#премиум-подписка)
5. [Система рейтинга](#система-рейтинга)
6. [Админ панель](#админ-панель)
7. [Карта и метки](#карта-и-метки)
8. [Дневник](#дневник)
9. [Как использовать](#как-использовать)

---

## 🎯 Обзор проекта

**FindShroom** - Android приложение для распознавания грибов с помощью AI (Gemini), картой с метками, справочником грибов, системой пользователей, премиум подпиской и рейтингом.

### Основные функции:
- 🔍 Распознавание грибов через Gemini AI
- 🗺️ Карта с метками найденных грибов
- 📚 Справочник грибов
- 👤 Система регистрации и авторизации
- ⭐ Премиум подписка по ключу
- 📊 Система рейтинга и уровней
- 📝 Личный дневник (для премиум)
- 🔒 Приватные метки (для премиум)
- 👨‍💼 Админ панель для модерации

---

## 🏗️ Архитектура

### Технологии:
- **Jetpack Compose** - декларативный UI
- **MVVM** - архитектура
- **Room Database** - локальное хранение данных
- **Hilt** - dependency injection
- **Navigation Compose** - навигация
- **StateFlow** - реактивное обновление UI
- **Yandex Maps SDK** - карта
- **Gemini AI** - распознавание грибов
- **Google Play Services Location** - определение местоположения

### Структура проекта:
```
app/src/main/java/com/example/findshroom/
├── data/
│   ├── dao/              # Room DAO интерфейсы
│   ├── database/         # Room Database
│   ├── model/            # Модели данных
│   └── repository/       # Репозитории
├── di/                   # Hilt модули
├── ui/
│   ├── navigation/       # Навигация
│   ├── screen/           # Экраны приложения
│   ├── theme/           # Тема
│   └── viewmodel/       # ViewModels
└── MainActivity.kt
```

---

## 👤 Система пользователей

### Регистрация и вход

**Экран:** `LoginScreen`

**Как зарегистрироваться:**
1. Откройте приложение
2. На экране входа нажмите "Нет аккаунта? Зарегистрироваться"
3. Введите имя пользователя и пароль
4. Нажмите "Зарегистрироваться"

**Как войти:**
1. Введите имя пользователя и пароль
2. Нажмите "Войти"

**Важно:** 
- Пароли хранятся в открытом виде (в реальном приложении должны быть захешированы)
- Каждый пользователь автоматически получает профиль со статистикой

### Модель User:
```kotlin
data class User(
    val id: Long = 0,
    val username: String,
    val password: String,
    val email: String? = null,
    val isAdmin: Boolean = false,  // Флаг администратора
    val createdAt: Long = System.currentTimeMillis()
)
```

### Хранение сессии:
- Используется `SharedPreferences` через `AuthRepository`
- Ключ: `"auth_prefs"` → `"current_user_id"`
- При выходе сессия очищается

---

## ⭐ Премиум подписка

### Как активировать подписку:

**Шаг 1: Получение ключа**
- Ключи подписки генерируются вручную (в текущей версии)
- Ключ - это любая строка, которая еще не была использована
- Примеры ключей: `"PREMIUM2024"`, `"SUBSCRIPTION_KEY_1"`, `"MY_PREMIUM_KEY"`

**Шаг 2: Активация**
1. Войдите в приложение
2. Откройте **Профиль** (иконка человека в верхнем правом углу)
3. Нажмите **"Активировать подписку"**
4. Введите ключ подписки
5. Нажмите **"Активировать"**

**Как это работает:**
- Система проверяет, не использован ли ключ ранее
- Если ключ уникален → подписка активируется
- Если ключ уже использован → ошибка "Неверный ключ подписки или ключ уже использован"
- Один ключ может быть использован только один раз

### Модель Subscription:
```kotlin
data class Subscription(
    val id: Long = 0,
    val userId: Long,              // ID пользователя
    val subscriptionKey: String,    // Ключ подписки
    val isActive: Boolean = true,   // Активна ли подписка
    val activatedAt: Long = System.currentTimeMillis()
)
```

### Премиум функции:
1. **Приватные метки** - метки видны только вам
2. **Личный дневник** - записывайте заметки о походах
3. **Система рейтинга** - получайте опыт и повышайте уровень

### Проверка подписки:
- Проверка происходит в `SubscriptionRepository.getActiveSubscription(userId)`
- Если у пользователя есть активная подписка → доступны премиум функции
- Метки фильтруются: бесплатные видят только публичные, премиум видят все

---

## 📊 Система рейтинга

### Уровни:

| Уровень | Название | Требуется опыта |
|---------|----------|----------------|
| 1-4 | Новичок | 0-400 |
| 5-9 | Любитель | 500-900 |
| 10-14 | Продвинутый грибник | 1000-1400 |
| 15-19 | Опытный грибник | 1500-1900 |
| 20+ | Профессиональный грибник | 2000+ |

### Как получать опыт:

**1. Создание меток:**
- Каждая созданная метка = +1 к счетчику меток
- Опыт не начисляется за метки

**2. Записи в дневнике (только для премиум):**
- Каждый собранный гриб = +10 опыта
- Пример: если указали 5 грибов → +50 опыта

**Формула опыта для следующего уровня:**
```
Опыт для следующего уровня = текущий_уровень × 100
```

**Пример:**
- Уровень 1 → нужно 100 опыта для уровня 2
- Уровень 5 → нужно 500 опыта для уровня 6
- Уровень 10 → нужно 1000 опыта для уровня 11

### Модель UserStats:
```kotlin
data class UserStats(
    val userId: Long,
    val experience: Int = 0,              // Текущий опыт
    val level: Int = 1,                   // Текущий уровень
    val totalMushroomsCollected: Int = 0, // Всего собрано грибов
    val totalMarkersCreated: Int = 0,     // Всего создано меток
    val lastUpdated: Long = System.currentTimeMillis()
)
```

### Где смотреть рейтинг:
- **Профиль** → карточка с информацией о пользователе
- Показывается: уровень, название уровня, опыт, прогресс до следующего уровня
- Статистика: собрано грибов, создано меток

---

## 👨‍💼 Админ панель

### Как создать админа:

**Важно:** В текущей версии админа нужно создать вручную через базу данных или код.

**Способ 1: Через код (для разработки)**
Добавьте в `FindShroomApplication.kt` или создайте временный скрипт:

```kotlin
// В onCreate() или отдельной функции
lifecycleScope.launch {
    val userRepository = // получить через DI
    val adminUser = User(
        username = "admin",
        password = "admin123",
        isAdmin = true
    )
    userRepository.insertUser(adminUser)
}
```

**Способ 2: Через базу данных (SQLite)**
1. Найдите файл базы данных: `findshroom_database` (обычно в `/data/data/com.example.findshroom/databases/`)
2. Откройте через SQLite Browser или adb shell
3. Выполните SQL:
```sql
UPDATE users SET isAdmin = 1 WHERE username = 'ваш_логин';
```

**Способ 3: Через регистрацию и обновление (временный)**
1. Зарегистрируйте пользователя через приложение
2. Найдите его ID в базе данных
3. Обновите поле `isAdmin = true` для этого пользователя

### Как войти в админ панель:

1. Войдите под учетной записью с `isAdmin = true`
2. Откройте **Профиль**
3. Внизу появится кнопка **"Админ панель"** (только для админов)
4. Нажмите на неё

### Функционал админ панели:

**Экран:** `AdminScreen`

**Что можно делать:**
1. **Просмотр всех меток** - видите все метки всех пользователей
2. **Удаление меток** - нажмите на иконку корзины рядом с меткой
3. **Модерация контента** - удаляйте неподходящие метки

**Ограничения:**
- Админ панель доступна только пользователям с `isAdmin = true`
- Если пользователь не админ → показывается "Доступ запрещен"
- Админ видит все метки (включая приватные)

### Модель доступа:
```kotlin
// Проверка в AdminViewModel
private fun checkAdminAccess() {
    val userId = authRepository.getCurrentUserId()
    val user = userRepository.getUserById(userId)
    _uiState.value = _uiState.value.copy(isAdmin = user?.isAdmin == true)
}
```

---

## 🗺️ Карта и метки

### Определение местоположения:

**Автоматически:**
- При открытии карты приложение автоматически определяет ваше местоположение
- Карта перемещается к вашей позиции (zoom 15)

**Вручную:**
- Нажмите кнопку с иконкой локации (внизу справа)
- Карта переместится к вашему текущему местоположению

**Разрешения:**
- `ACCESS_FINE_LOCATION` - точное местоположение
- `ACCESS_COARSE_LOCATION` - приблизительное местоположение

### Создание меток:

**Способ 1: Долгое нажатие на карте**
1. Нажмите и удерживайте на карте в нужном месте
2. Откроется диалог добавления метки

**Способ 2: Кнопка "+"**
1. Переместите карту к нужному месту
2. Нажмите кнопку "+" (внизу справа)
3. Откроется диалог добавления метки

**Диалог добавления метки:**
- **Название** (необязательно) - название метки
- **Заметка** (необязательно) - описание
- **Фото** (обязательно) - выберите фото из галереи
- **Приватная метка** (только для премиум) - чекбокс для приватных меток

**После создания:**
- Метка сразу появляется на карте
- Видна всем (если не приватная) или только вам (если приватная)

### Просмотр меток:

**На карте:**
- Все видимые метки отображаются как маркеры
- Нажмите на маркер → откроется диалог с деталями

**Диалог деталей метки:**
- Показывает координаты
- Можно редактировать название и заметку
- Можно удалить метку (кнопка "Удалить")

### Фильтрация меток:

**Бесплатные пользователи:**
- Видят только публичные метки (`isPrivate = false`)
- Не видят приватные метки других пользователей

**Премиум пользователи:**
- Видят все метки (публичные и приватные)
- Могут создавать приватные метки

### Модель MapMarker:
```kotlin
data class MapMarker(
    val id: Long = 0,
    val latitude: Double,
    val longitude: Double,
    val photoUri: String,        // URI фото
    val title: String? = null,   // Название метки
    val mushroomId: Long? = null, // Связь с грибом из справочника
    val note: String? = null,     // Заметка
    val userId: Long? = null,     // ID создателя
    val isPrivate: Boolean = false, // Приватная ли метка
    val timestamp: Long = System.currentTimeMillis()
)
```

---

## 📝 Дневник

### Доступ:
- **Только для премиум пользователей**
- Открывается из **Профиля** → кнопка **"Личный дневник"**

### Как использовать:

**Создание записи:**
1. Откройте дневник
2. Нажмите кнопку "+" (внизу справа)
3. Заполните форму:
   - **Заметка** (обязательно) - описание похода/находки
   - **Количество грибов** (необязательно) - сколько собрали
4. Нажмите **"Добавить"**

**Просмотр записей:**
- Все записи отображаются в хронологическом порядке (новые сверху)
- Показывается дата и время создания
- Показывается заметка
- Показывается количество собранных грибов (если указано)

**Начисление опыта:**
- При указании количества грибов → автоматически начисляется опыт
- Формула: `количество_грибов × 10 опыта`
- Пример: 5 грибов → +50 опыта

### Модель DiaryEntry:
```kotlin
data class DiaryEntry(
    val id: Long = 0,
    val userId: Long,              // ID пользователя
    val note: String,               // Заметка
    val mushroomsCollected: Int = 0, // Количество грибов
    val timestamp: Long = System.currentTimeMillis()
)
```

---

## 🚀 Как использовать

### Первый запуск:

1. **Запустите приложение**
2. **Зарегистрируйтесь** или войдите
3. **Разрешите доступ к местоположению** (для карты)
4. **Разрешите доступ к камере/галерее** (для фото)

### Основной workflow:

**1. Распознавание грибов:**
- Откройте вкладку "Распознавание"
- Сделайте фото или выберите из галереи
- Дождитесь результата от Gemini AI
- Сохраните в справочник при необходимости

**2. Добавление меток на карте:**
- Откройте вкладку "Карта"
- Найдите место на карте
- Создайте метку (долгое нажатие или кнопка "+")
- Добавьте фото, название, заметку
- Для премиум: отметьте как приватную

**3. Просмотр справочника:**
- Откройте вкладку "Справочник"
- Ищите грибы по названию
- Просматривайте детали

**4. Активация премиум:**
- Откройте Профиль
- Нажмите "Активировать подписку"
- Введите ключ

**5. Использование дневника (премиум):**
- Откройте Профиль
- Нажмите "Личный дневник"
- Добавляйте записи о походах
- Указывайте количество грибов для опыта

**6. Админ панель (только для админов):**
- Войдите под админ аккаунтом
- Откройте Профиль
- Нажмите "Админ панель"
- Модерируйте метки

---

## 🔧 Технические детали

### База данных:

**Версия:** 3

**Таблицы:**
- `users` - пользователи
- `subscriptions` - подписки
- `user_stats` - статистика пользователей
- `diary_entries` - записи дневника
- `map_markers` - метки на карте
- `mushrooms` - справочник грибов

**Миграции:**
- Версия 1 → 2: добавлены поля `title`, `userId`, `isPrivate` в `MapMarker`
- Версия 2 → 3: добавлены таблицы `users`, `subscriptions`, `user_stats`, `diary_entries`

### Хранение данных:

**Локальное:**
- Все данные хранятся в Room Database
- Сессия пользователя в SharedPreferences
- Фото хранятся как URI (не копируются в приложение)

**Без серверной части:**
- Все работает локально
- Нет синхронизации между устройствами
- Ключи подписки проверяются локально

### Безопасность:

**Текущие ограничения:**
- Пароли хранятся в открытом виде (⚠️ нужно хешировать)
- Ключи подписки можно использовать только один раз
- Админ доступ проверяется через флаг `isAdmin`

**Рекомендации для продакшена:**
- Хешировать пароли (BCrypt, Argon2)
- Шифровать чувствительные данные
- Добавить серверную валидацию ключей
- Добавить токены авторизации

---

## 📱 Экраны приложения

### 1. LoginScreen (Экран входа)
- Регистрация/Вход
- Валидация полей
- Обработка ошибок

### 2. RecognitionScreen (Распознавание)
- Фото гриба
- Распознавание через Gemini AI
- Сохранение в справочник

### 3. MapScreen (Карта)
- Yandex карта
- Определение местоположения
- Создание/просмотр меток
- Фильтрация приватных меток

### 4. GuideScreen (Справочник)
- Список грибов
- Поиск
- Детали гриба
- Редактирование/удаление

### 5. ProfileScreen (Профиль)
- Информация о пользователе
- Статистика и рейтинг
- Активация подписки
- Доступ к дневнику
- Доступ к админ панели (для админов)
- Выход

### 6. SubscriptionScreen (Подписка)
- Ввод ключа подписки
- Активация
- Обработка ошибок

### 7. DiaryScreen (Дневник)
- Список записей
- Создание записи
- Начисление опыта

### 8. AdminScreen (Админ панель)
- Список всех меток
- Удаление меток
- Проверка доступа

---

## 🎯 Примеры использования

### Пример 1: Обычный пользователь

1. Регистрация: `username: "user1"`, `password: "pass123"`
2. Вход в приложение
3. Создание меток на карте (публичные)
4. Просмотр меток других пользователей
5. Нет доступа к дневнику (нужна подписка)

### Пример 2: Премиум пользователь

1. Регистрация и вход
2. Активация подписки: ключ `"PREMIUM2024"`
3. Создание приватных меток
4. Использование дневника
5. Получение опыта за записи в дневнике
6. Повышение уровня

### Пример 3: Администратор

1. Создание админ аккаунта (через БД или код)
2. Вход в приложение
3. Доступ к админ панели
4. Модерация меток пользователей
5. Удаление неподходящих меток

---

## 📝 Важные заметки

1. **Ключи подписки** генерируются вручную и хранятся локально
2. **Админы** создаются вручную через БД или код
3. **Пароли** хранятся в открытом виде (нужно исправить)
4. **Все данные** хранятся локально, нет синхронизации
5. **Приватные метки** видны только создателю (если у него премиум)
6. **Опыт** начисляется только за записи в дневнике с указанием грибов
7. **Метки** видны сразу после создания (через Flow)

---

## 🔄 Обновления и улучшения

### Планируемые улучшения:
- [ ] Хеширование паролей
- [ ] Серверная валидация ключей подписки
- [ ] Синхронизация данных между устройствами
- [ ] Улучшенная система модерации
- [ ] Статистика по меткам
- [ ] Экспорт данных дневника
- [ ] Уведомления о новых метках
- [ ] Социальные функции (комментарии, лайки)

---

**Версия документации:** 1.0  
**Дата обновления:** 2024  
**Автор:** FindShroom Development Team

